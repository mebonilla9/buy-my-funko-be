server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/buy_my_funko'
    username: 'funko'
    password: 'Funko1234!'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show_sql: true